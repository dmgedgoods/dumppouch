import random 
messages = ['Unknown',
            'Eat shit',
            'Maybe later',
            'Reply hazy, try again',
            'Not looking good, buddy']
print(messages[random.randint(0, len(messages) - 1)])
